# 2026.8.23

## English

### Goals for the Day

- Align the replaceable public example content with the localized eight-page framework.
- Keep the Simplified Chinese base complete while allowing English content to be added independently and incrementally.
- Preserve a neutral public template with no GHFRC private text or media.

### Completed Work

- Moved all eight complete example page documents into `pages/zh-CN/` and added matching independent English overlay documents under `pages/en/`.
- Split site-wide configuration into `config/locales/zh-CN/site.yaml` and `config/locales/en/site.yaml` and removed the obsolete About FRC locale configuration files.
- Added stable IDs to repeatable highlights, partner items, and source items so English overlays merge by identity.
- Added localized interface text for language navigation, achievement notifications, and placeholder categories while intentionally leaving example page translations incomplete.
- Updated the public content guide and validation requirements for the localized directory structure and typed image and video media.
- Reported `141` missing English values without blocking checks or builds and confirmed that the generated English pages remain protected with `noindex`.
- Confirmed that the public example build stages only the neutral `400 × 138 px` placeholder Logo and contains no GHFRC private media or internal references.

## 中文

### 当日目标

- 使可替换的公开示例内容与本地化 8 页面框架保持一致。
- 保持简体中文基础内容完整，同时允许英文内容相互独立并逐步补充。
- 保持中性的公开模板，不包含 GHFRC 私有文字或媒体。

### 当日完成

- 将全部 8 个完整示例页面文档移入 `pages/zh-CN/`，并在 `pages/en/` 下新增对应的独立英文覆盖文档。
- 将全站配置拆分为 `config/locales/zh-CN/site.yaml` 与 `config/locales/en/site.yaml`，并删除过期的“关于 FRC”语言配置文件。
- 为可重复的数据亮点、合作机构项目和资料来源项目加入稳定 ID，使英文覆盖按内容身份合并。
- 为语言导航、成就通知和占位类别加入本地化界面文字，同时有意保持示例页面翻译未完成状态。
- 为本地化目录结构及带类型的图片和视频媒体更新公开内容指南与校验要求。
- 报告 `141` 个缺失英文值而不阻断检查或构建，并确认生成的英文页面继续通过 `noindex` 防止收录。
- 确认公开示例构建只暂存中性的 `400 × 138 px` 占位 Logo，不包含 GHFRC 私有媒体或内部参考资料。
