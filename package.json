{
  "name": "gh-frc-website",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  },
  "workspaces": [
    "achievements",
    "framework"
  ],
  "scripts": {
    "prepare:content": "node scripts/prepare-content.mjs",
    "dev": "npm run prepare:content && npm run check:translations && npm --workspace @gh-frc/framework run dev",
    "check:translations": "node scripts/check-translations.mjs",
    "build": "npm run prepare:content && npm run check:translations && npm --workspace @gh-frc/framework run build",
    "check": "npm run prepare:content && npm run check:translations && npm run check --workspaces --if-present",
    "preview": "npm run prepare:content && npm --workspace @gh-frc/framework run preview",
    "test": "npm run test --workspaces --if-present"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
