# 2026.8.23

## English

### Goals for the Day

- Integrate the Simplified Chinese and English system with the latest eight-page `preview` architecture without losing either development history.
- Keep the public example content, private GHFRC content, framework, skins, and achievements isolated while preparing both repositories for Preview.
- Verify routing, language selection, incomplete-translation fallback, search metadata, content privacy, and generated output before merging.

### Completed Work

- Merged the latest `preview` histories into both multilingual feature branches and reconciled the former localized single-page implementation with the eight-page website.
- Generated localized home pages and all eight primary pages under `/zh-cn/` and `/en/`, with automatic language entry routes and localized robot and news detail-route scaffolding.
- Added persistent manual language selection, Chinese-browser-language mapping to Simplified Chinese, English fallback for every non-Chinese browser language, and language switching that preserves the current page, query, and fragment.
- Reorganized both public and private content into complete Simplified Chinese bases and independent English overlays while retaining strict public-private media separation.
- Added field-level structured-content fallback, stable-ID list merging, Simplified Chinese fallback for empty English Markdown bodies, fallback language semantics, translation reporting, localized canonical and `hreflang` metadata, and automatic `noindex` protection for incomplete English pages.
- Updated content preparation, schemas, route verification, tests, READMEs, and implementation specifications for the final localized multi-page structure.
- Verified `116` automated tests, Astro diagnostics with no errors, warnings, or hints, public and private content checks, both static builds, `28` generated pages, `34` public output files, and `39` private output files.
- Confirmed that the public build contains only the replaceable example Logo and that the private build uses the unchanged real school Logo without staging internal reference files.

## 中文

### 当日目标

- 将简体中文与英文系统整合进最新的 8 页面 `preview` 架构，同时保留两边的开发历史。
- 在准备两个仓库的 Preview 版本时，继续保持公开示例内容、GHFRC 私有内容、框架、皮肤和成就系统相互独立。
- 在合并前验证路由、语言选择、未完成翻译回退、搜索元数据、内容隐私和生成产物。

### 当日完成

- 将两个仓库最新的 `preview` 历史合并进多语言功能分支，并把原有本地化单页实现与 8 页面官网完成整合。
- 在 `/zh-cn/` 与 `/en/` 下生成本地化首页及全部 8 个主要页面，并加入自动语言入口和本地化机器人、新闻详情路由框架。
- 加入长期保存的手动语言选择、中文浏览器语言统一映射到简体中文、所有非中文浏览器语言统一进入英文，以及保留当前页面、查询参数和页面片段的语言切换。
- 将公开与私有内容重组为完整简体中文基础内容和独立英文覆盖内容，同时保持严格的公开／私有媒体边界。
- 加入结构化内容逐字段回退、按稳定 ID 合并列表、英文 Markdown 正文为空时的简体中文回退、回退语言语义、翻译报告、本地化规范链接与 `hreflang` 元数据，以及英文未完成时的自动 `noindex` 保护。
- 为最终的本地化多页面结构更新内容准备流程、Schema、路由产物验证、测试、README 和实施规格。
- 通过 `116` 项自动化测试、无错误／警告／提示的 Astro 诊断、公开与私有内容检查、两套静态构建、`28` 个生成页面、`34` 个公开输出文件和 `39` 个私有输出文件的验证。
- 确认公开构建只包含可替换的示例 Logo，私有构建使用未经修改的真实学校 Logo，且没有暂存内部参考文件。
