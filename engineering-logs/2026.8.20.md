# 2026.8.20

## English

### Goals for the Day

- Audit the About FRC feature logs and release readiness before publishing both repositories to `preview` through Pull Requests.
- Align the implementation specification with the approved transition from a single long page to a long home page plus independent content pages.
- Establish the Simplified Chinese and English website foundation while preserving a clear path for a future unified Traditional Chinese version.
- Keep language routing, incomplete-translation fallback, search indexing, localized media, and the public-private content boundary predictable and verifiable.

### Completed Work

- Confirmed that the existing overall, framework, public example-content, and private real-content logs cover every module changed by the About FRC feature and follow the required bilingual same-day format.
- Re-ran all 53 automated tests, Astro checks, and builds with both public example content and private GHFRC content successfully.
- Updated the first-version implementation specification to define `/about-frc/` as an independent page, preserve the remaining home-page anchors, and distinguish Logo behavior between the home page and independent pages.
- Corrected the framework documentation so it no longer describes the complete website as a single static page.
- Re-reviewed the two feature branches against the implementation requirements, repository standards, content privacy boundary, and Pull Request release order without finding any remaining merge blocker.
- Added explicit `/zh-cn/` and `/en/` routes for the home page and About FRC page, plus automatic language entry routes that prioritize a saved manual choice before the browser's primary language.
- Mapped every Chinese browser language, including Traditional Chinese variants, to Simplified Chinese and mapped English, non-Chinese, empty, or invalid browser languages to English.
- Added a header language control that remembers only manual or explicit choices, keeps browser-detected entry automatic on later visits, preserves the same English page slug, query string, and page fragment, and allows explicit language URLs to remain authoritative.
- Reorganized public example content and private GHFRC content into complete Simplified Chinese bases and independent English overlays that merge by stable field and block identifiers.
- Added field-level and paragraph-level Simplified Chinese fallback for incomplete English content without visitor-facing notices, while marking fallback nodes with their actual language for assistive technology.
- Added localized metadata, canonical URLs, `hreflang` alternates, automatic `noindex` protection for incomplete English pages, and deployment-wide `noindex` protection for Preview builds.
- Added a non-blocking missing-English report and locale-aware media staging that collects only referenced deployable files and rejects internal-reference paths.
- Made the optional achievement notification text localizable without changing the achievement module's default behavior.
- Verified 99 automated tests, Astro diagnostics, translation reports, and six-route static builds with both the public example source and private GHFRC content source; confirmed that internal reference media did not enter either build output.

## 中文

### 当日目标

- 在通过 Pull Request 将两个仓库发布到 `preview` 前，审核“关于 FRC”功能的工程日志和发布准备状态。
- 使实施规格与已经确认的“长首页加独立内容页面”演进方向保持一致。
- 建立简体中文与英文网站基础，同时为未来统一的繁体中文版本保留清晰扩展路径。
- 使语言路由、未完成翻译回退、搜索引擎收录、本地化媒体和公开／私有内容边界保持可预测且可验证。

### 当日完成

- 确认现有总工程日志、framework 日志、公开示例 content 日志和私有真实 content 日志覆盖“关于 FRC”功能涉及的全部模块，并符合双语同日记录格式。
- 使用公开示例内容和 GHFRC 私有内容重新运行全部 53 项自动化测试、Astro 检查与构建，结果全部通过。
- 更新第一版实施规格，将 `/about-frc/` 定义为独立页面，保留其余首页锚点，并区分首页与独立页面中的 Logo 行为。
- 修正 framework 文档，使其不再将完整网站描述为单一静态页面。
- 按实施要求、仓库规范、内容隐私边界和 Pull Request 发布顺序重新审查两个功能分支，未发现剩余的合并阻断项。
- 为首页和“关于 FRC”页面新增明确的 `/zh-cn/` 与 `/en/` 路由，并加入优先读取已保存手动选择、其次读取浏览器首选语言的自动语言入口。
- 将所有中文浏览器语言（包括繁体中文变体）映射到简体中文，将英文、非中文、空值或无效浏览器语言映射到英文。
- 在标题栏加入语言切换控件，只长期记住手动选择或明确 URL 选择，使浏览器自动判断在后续访问中仍保持自动，保留相同的英文页面 slug、查询参数和页面片段，并确保明确语言 URL 始终优先。
- 将公开示例内容和 GHFRC 私有内容重组为完整简体中文基础内容与独立英文覆盖层，并通过稳定的字段及内容块标识进行合并。
- 为尚未完成的英文内容加入字段级和段落级简体中文回退，不向访客显示提示，并为辅助技术标注回退节点的实际语言。
- 新增本地化元数据、规范链接、`hreflang` 对应关系、英文内容未完成时的自动 `noindex` 保护，以及 Preview 构建的全站 `noindex` 保护。
- 新增不会阻断构建的英文缺失报告，以及仅收集已引用可部署文件并拒绝内部参考路径的多语言媒体暂存流程。
- 使可选成就通知文字支持本地化，同时保持成就模块的默认行为不变。
- 使用公开示例来源和 GHFRC 私有内容来源验证 99 项自动化测试、Astro 诊断、翻译报告和六路由静态构建，并确认内部参考媒体未进入任何构建产物。
