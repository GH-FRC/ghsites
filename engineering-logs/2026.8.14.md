# 2026.8.14

## English

### Goals for the Day

- Show the Stable and Preview website deployments on both the public and private GitHub repository home pages.
- Keep private content and Cloudflare credentials outside the public repository while synchronizing deployment status.

### Completed Work

- Confirmed that the private repository already recorded successful `production` and `preview` deployments.
- Added public deployment tracking to the Stable workflow without granting the public repository access to private content or Cloudflare credentials.
- Configured the public workflow to wait for the private deployment result before reporting success or failure.
- Reused the existing private-workflow trigger credential and did not add a broader cross-repository credential.

## 中文

### 当日目标

- 在公开和私有 GitHub 仓库主页中同时显示 Stable 与 Preview 官网 Deployment。
- 在同步部署状态的同时，避免私有内容和 Cloudflare 凭据进入公开仓库。

### 当日完成

- 确认私有仓库已经记录成功的 `production` 与 `preview` Deployment。
- 为 Stable 工作流加入公开 Deployment 跟踪，同时不向公开仓库授予私有内容或 Cloudflare 凭据的访问权限。
- 配置公开工作流，使其等待私有部署结果后再报告成功或失败。
- 复用现有私有工作流触发凭据，没有新增权限范围更大的跨仓库凭据。
