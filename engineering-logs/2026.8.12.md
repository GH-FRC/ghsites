# 2026.8.12

## English

### Goals for the Day

- Reorganize the website into a public reusable codebase and a separate private real-content repository.
- Preserve the complete legacy local repository, including its Git history, branches, stash, and uncommitted work.
- Provide a same-size neutral Logo placeholder without exposing the original school brand asset.
- Verify that both public example content and private GHFRC content remain valid build inputs.
- Preserve the complete legacy development history while keeping every real-content revision private.
- Recover and correctly classify the planning document and internal UI reference that existed only in the legacy Git stash.
- Keep the header visible during navigation-triggered scrolling so visitors can select consecutive sections.
- Add a desktop color-scheme control that follows the visitor's system preference and keeps a manual choice only for the current browsing session.

### Completed Work

- Preserved the original project as `GHFRC Website Legacy Backup` without changing its Git data or working files.
- Created a local workspace containing the public `ghfrc-website` repository and the private `ghfrc-website-content` repository as sibling folders.
- Migrated the current framework, skins, documentation, scripts, engineering logs, and example content into the clean public repository without carrying over the private repository's Git history.
- Migrated the original `content` files into the private content repository and verified that the original `400 × 138 px` Logo remains byte-identical to the legacy source.
- Replaced the public Logo with a neutral `400 × 138 px` placeholder containing no GHFRC or school brand asset.
- Added automatic local content-source selection with explicit configuration first, the sibling private content repository second, and public example content as the fallback.
- Made the Logo media path content-configurable so the public example can use `placeholder-logo.png` while the private content keeps its unchanged original filename.
- Added public template documentation, private content safety documentation, local-secret ignore rules, and an MIT License for the public repository.
- Verified nine passing behavior tests, zero Astro diagnostics, and successful builds with both public example content and private GHFRC content.
- Created the clean public repository's first commit and pushed its `main` branch to `GH-FRC/ghfrc-website`.
- Kept the header visible during navigation-triggered smooth scrolling so visitors can select consecutive sections without first scrolling upward.
- Preserved the existing rule that deliberate downward scrolling hides the header and upward scrolling restores it.
- Restored normal header direction handling when keyboard or pointer input interrupts navigation-triggered smooth scrolling.
- Kept navigation state active when another header section link is pressed during an unfinished smooth scroll, preventing the header from disappearing before the next click completes.
- Split the four legacy commits into sanitized public history and private real-content history while preserving their author, timestamps, messages, order, and contribution attribution.
- Connected the sanitized legacy framework and skin history to the public `Tony` branch without changing any existing public repository commit.
- Verified that no real content file, original Logo blob, or internal UI reference video is reachable from the public repository history.
- Restored the complete website planning document from the legacy Git stash, renamed it `website-architecture-and-implementation-plan.md`, updated it to match the current two-repository architecture, and placed it under `docs/`.
- Restored the internal UI reference video from the legacy stash, renamed it `particle-skin-ui-reference.mov`, and stored it through Git LFS in the private content repository.
- Audited the legacy working tree, ignored files, and stash, confirming that all non-generated project material has a destination in the new repositories.
- Pushed both migrated `Tony` histories, verified the remote branch tips, and moved the legacy local backup to the macOS Trash only after both new repositories were clean and synchronized.
- Completed independent standards and requirements reviews of the migration, resolving the documentation findings before the final `Tony` updates.
- Added a global light and dark color-scheme system that follows `prefers-color-scheme` until the visitor makes a manual selection.
- Placed an accessible `44 × 44 px` sun and moon control at the far right of the desktop header, following the restrained global-utility placement observed across the selected official reference websites.
- Stored manual color-scheme choices in `sessionStorage`, restored them before the first paint without a theme flash, and returned to the system preference in a new browsing session.
- Added semantic dark-mode design tokens for page surfaces, borders, text, the footer, header control feedback, and the unchanged transparent Logo's white backing surface.
- Added nine color-scheme tests, including direct coverage of the pre-paint bootstrap for valid, invalid, and unavailable session storage, and verified eighteen total tests, zero Astro diagnostics, successful private-content and public-example builds, and the rendered desktop interaction in a real browser.
- Consolidated the light and dark design tokens into one `light-dark()` source and made the base layout reuse the tested bootstrap implementation, resolving the final independent review findings.

## 中文

### 当日目标

- 将官网重新整理为公开可复用代码库和独立的私有真实内容仓库。
- 完整保留旧本地仓库的 Git 历史、分支、stash 和未提交工作。
- 在不公开原始学校品牌资产的前提下，提供同尺寸的中性 Logo 占位图。
- 验证公开示例内容和 GHFRC 私有内容均可作为有效构建来源。
- 在确保所有真实内容版本保持私有的前提下，保留完整旧开发历史。
- 恢复只存在于旧 Git stash 中的规划文档和内部 UI 参考，并按公开范围正确归档。
- 在导航触发的滚动期间保持标题栏可见，使访客能够连续选择栏目。
- 增加桌面端深色／浅色模式控件，默认跟随访客系统设置，并只在当前浏览会话中保留手动选择。

### 当日完成

- 将原项目完整保留为 `GHFRC Website Legacy Backup`，没有修改其中的 Git 数据或工作文件。
- 建立本地总工作区，并将公开的 `ghfrc-website` 仓库和私有的 `ghfrc-website-content` 仓库作为同级文件夹保存。
- 将当前框架、皮肤、文档、脚本、工程日志和示例内容迁入干净的公开仓库，没有带入私有旧仓库的 Git 历史。
- 将原始 `content` 文件迁入私有内容仓库，并确认原始 `400 × 138 px` Logo 与旧项目源文件逐字节一致。
- 将公开仓库中的 Logo 替换为不含 GHFRC 或学校品牌资产的 `400 × 138 px` 中性占位图。
- 加入本地内容来源自动选择机制，优先使用明确配置，其次使用同级私有内容仓库，最后回退到公开示例内容。
- 将 Logo 媒体路径改为由内容配置决定，使公开示例可以使用 `placeholder-logo.png`，同时私有内容继续保留未修改的原始文件名。
- 加入公开模板说明、私有内容安全说明、本地敏感配置忽略规则和公开仓库的 MIT License。
- 验证 9 项行为测试通过、Astro 诊断为 0，并确认公开示例内容和 GHFRC 私有内容均可成功构建。
- 创建干净公开仓库的首次提交，并将其 `main` 分支推送至 `GH-FRC/ghfrc-website`。
- 在导航触发的平滑滚动期间保持标题栏可见，使访客无需先向上滚动即可连续选择栏目。
- 保留用户主动向下滚动时隐藏标题栏、向上滚动时恢复标题栏的原有规则。
- 当键盘或指针输入中断导航触发的平滑滚动时，恢复标题栏正常的方向判断逻辑。
- 在一次平滑滚动尚未结束时按下另一个标题栏栏目链接，继续保持导航状态，避免标题栏在下一次点击完成前消失。
- 将旧仓库的 4 个提交拆分为经过净化的公开历史和真实内容私有历史，并保留作者、时间、提交说明、顺序及贡献归属。
- 将经过净化的旧框架与皮肤历史连接到公开仓库的 `Tony` 分支，未改变公开仓库任何已有提交。
- 验证公开仓库历史无法访问真实内容文件、原始 Logo 对象或内部 UI 参考视频。
- 从旧 Git stash 完整恢复官网规划文档，将其重命名为 `website-architecture-and-implementation-plan.md`，按当前双仓库架构更新后放入 `docs/`。
- 从旧 stash 恢复内部 UI 参考视频，将其重命名为 `particle-skin-ui-reference.mov`，并通过 Git LFS 保存到私有内容仓库。
- 审计旧工作区、忽略文件和 stash，确认除可重新生成内容外的全部项目资料在新仓库中都有对应位置。
- 推送两个迁移后的 `Tony` 历史并核对远程分支端点；确认两个新仓库均干净且同步后，才将旧本地备份移入 macOS 废纸篓。
- 对迁移结果完成独立的规范审查与需求审查，并在最终更新 `Tony` 前处理审查发现的文档问题。
- 加入全局深色／浅色模式系统，在访客未手动选择时跟随 `prefers-color-scheme`。
- 将具备无障碍语义的 `44 × 44 px` 太阳／月亮控件放在桌面端标题栏最右侧，并参考所选官方网站中克制的全局工具布局方式。
- 使用 `sessionStorage` 保存手动模式选择，在首屏绘制前恢复选择以避免主题闪烁，并在新的浏览会话中重新跟随系统。
- 为页面表面、边框、文字、页脚、标题栏控件反馈和未修改透明 Logo 的白色承托表面加入语义化深色设计令牌。
- 新增 9 项颜色模式测试，其中直接覆盖首屏绘制前初始化在会话存储值有效、无效及存储不可用时的行为；同时确认共 18 项测试通过、Astro 诊断为 0、私有内容和公开示例内容均构建成功，以及真实浏览器中的桌面端交互正确。
- 使用单一 `light-dark()` 来源统一深色与浅色设计令牌，并让基础布局复用经过测试的首屏初始化实现，处理完最终独立审查发现的问题。
