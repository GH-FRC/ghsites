# 2026.8.12

## English

### Goals for the Day

- Reorganize the website into a public reusable codebase and a separate private real-content repository.
- Preserve the complete legacy local repository, including its Git history, branches, stash, and uncommitted work.
- Provide a same-size neutral Logo placeholder without exposing the original school brand asset.
- Verify that both public example content and private GHFRC content remain valid build inputs.

### Completed Work

- Preserved the original project as `GHFRC Website Legacy Backup` without changing its Git data or working files.
- Created a local workspace containing the public `ghfrc-website` repository and the private `ghfrc-website-content` repository as sibling folders.
- Migrated the current framework, skins, documentation, scripts, engineering logs, and example content into the clean public repository without carrying over the private repository's Git history.
- Migrated the original `content` files into the private content repository and verified that the original `400 × 138 px` Logo remains byte-identical to the legacy source.
- Replaced the public Logo with a neutral `400 × 138 px` placeholder containing no GHFRC or school brand asset.
- Added automatic local content-source selection with explicit configuration first, the sibling private content repository second, and public example content as the fallback.
- Made the Logo media path content-configurable so the public example can use `placeholder-logo.png` while the private content keeps its unchanged original filename.
- Added public template documentation, private content safety documentation, local-secret ignore rules, and an MIT License for the public repository.
- Verified five passing behavior tests, zero Astro diagnostics, and successful builds with both public example content and private GHFRC content.
- Created the clean public repository's first commit and pushed its `main` branch to `GH-FRC/ghfrc-website`.

## 中文

### 当日目标

- 将官网重新整理为公开可复用代码库和独立的私有真实内容仓库。
- 完整保留旧本地仓库的 Git 历史、分支、stash 和未提交工作。
- 在不公开原始学校品牌资产的前提下，提供同尺寸的中性 Logo 占位图。
- 验证公开示例内容和 GHFRC 私有内容均可作为有效构建来源。

### 当日完成

- 将原项目完整保留为 `GHFRC Website Legacy Backup`，没有修改其中的 Git 数据或工作文件。
- 建立本地总工作区，并将公开的 `ghfrc-website` 仓库和私有的 `ghfrc-website-content` 仓库作为同级文件夹保存。
- 将当前框架、皮肤、文档、脚本、工程日志和示例内容迁入干净的公开仓库，没有带入私有旧仓库的 Git 历史。
- 将原始 `content` 文件迁入私有内容仓库，并确认原始 `400 × 138 px` Logo 与旧项目源文件逐字节一致。
- 将公开仓库中的 Logo 替换为不含 GHFRC 或学校品牌资产的 `400 × 138 px` 中性占位图。
- 加入本地内容来源自动选择机制，优先使用明确配置，其次使用同级私有内容仓库，最后回退到公开示例内容。
- 将 Logo 媒体路径改为由内容配置决定，使公开示例可以使用 `placeholder-logo.png`，同时私有内容继续保留未修改的原始文件名。
- 加入公开模板说明、私有内容安全说明、本地敏感配置忽略规则和公开仓库的 MIT License。
- 验证 5 项行为测试通过、Astro 诊断为 0，并确认公开示例内容和 GHFRC 私有内容均可成功构建。
- 创建干净公开仓库的首次提交，并将其 `main` 分支推送至 `GH-FRC/ghfrc-website`。
