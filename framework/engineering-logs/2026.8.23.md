# 2026.8.23

## English

### Goals for the Day

- Move the Logo to the left and the compact-menu button to the right on phone and narrow iPad layouts without changing the desktop header.
- Retain the existing color-scheme control, accessible control sizes, proportional Logo display, and compact-menu behavior.

### Completed Work

- Reordered the shared header markup to keep keyboard focus order aligned with the new compact-layout visual order.
- Changed the compact header grid to place the Logo in the flexible left column and the color-scheme and menu controls in two fixed right columns.
- Preserved the desktop grid, responsive breakpoint, Logo source file, navigation interactions, and hide-on-downscroll and return-on-upscroll behavior.
- Verified the framework with `40` framework tests, repository-wide checks and tests, public and private builds, generated-output checks, and real-browser measurements at `320`, `390`, `768`, `834`, `1024`, `1100`, `1101`, and `1440 px`.

## 中文

### 当日目标

- 在不改变电脑端标题栏的前提下，将手机和窄屏 iPad 布局中的 Logo 移到左侧，并将紧凑菜单按钮移到右侧。
- 保留现有颜色模式控件、无障碍控件尺寸、Logo 等比例显示和紧凑菜单行为。

### 当日完成

- 调整共用标题栏标记顺序，使键盘焦点顺序与新的紧凑布局视觉顺序保持一致。
- 修改紧凑标题栏网格，将 Logo 放入左侧弹性列，并将颜色模式控件和菜单按钮放入右侧两个固定列。
- 保持电脑端网格、响应式断点、Logo 源文件、导航交互，以及向下滚动隐藏和向上滚动返回标题栏的行为不变。
- 使用 `40` 项 framework 测试、全仓库检查与测试、公开与私有构建、生成产物检查，以及 `320`、`390`、`768`、`834`、`1024`、`1100`、`1101` 和 `1440 px` 的真实浏览器测量验证框架。
