# 2026.8.23

## English

### Goals for the Day

- Port the approved language behavior to the complete multi-page framework.
- Preserve the latest responsive navigation, color-scheme behavior, media support, and legacy links during localization.
- Make every generated locale route, fallback state, and search metadata rule automatically verifiable.
- Keep the Logo on the left and the compact-menu button on the right in phone and narrow iPad layouts while inserting the language control.

### Completed Work

- Reworked the framework's locale loader to combine complete Simplified Chinese site and page documents with independent English overlays across all eight primary pages.
- Added localized home, primary-page, robot-detail, and news-detail routes, plus automatic unprefixed entry routes for the same English slugs.
- Adapted the shared header and page layouts so the language control appears beside the color-scheme control on every page and preserves the destination page, query, and fragment.
- Retained automatic browser-language selection, persistent manual language choice, explicit localized-URL precedence, and the agreed mapping of all non-Chinese languages to English.
- Integrated structured-content fallback tracking, stable-ID arrays, Markdown-body fallback semantics, accessible fallback language markers, localized placeholders, canonical links, `hreflang`, and incomplete-English `noindex` output.
- Updated legacy Hash and `/about-frc/` redirects to enter the matching localized routes.
- Expanded schemas and rendering for typed image and video media, optional posters, and caption tracks without allowing internal references into deployable output.
- Reordered the compact header markup and grid so its focus and visual order is Logo, language control, color-scheme control, then menu button.
- Preserved the desktop grid, `1100 px` responsive boundary, proportional Logo sizing, existing menu interactions, and hide-on-downscroll and return-on-upscroll behavior.
- Passed `86` framework tests, Astro diagnostics with no errors, warnings, or hints, and generated-output verification for all `28` current pages using both public and private content.

## 中文

### 当日目标

- 将已确认的语言行为迁移到完整多页面框架。
- 在本地化过程中保留最新的响应式导航、深浅色行为、媒体支持和旧链接兼容。
- 使每个生成语言路由、回退状态和搜索元数据规则都能够自动验证。
- 在插入语言控件的同时，保持手机和窄屏 iPad 布局中的 Logo 位于左侧、紧凑菜单按钮位于右侧。

### 当日完成

- 重构框架的语言内容加载器，在全部 8 个主要页面中组合完整简体中文全站及页面文档与独立英文覆盖内容。
- 新增本地化首页、主要页面、机器人详情和新闻详情路由，并为相同英文 slug 加入无语言前缀自动入口。
- 调整共用标题栏和页面布局，使语言控件在每个页面中位于深浅色控件旁，并在切换时保留目标页面、查询参数和页面片段。
- 保留浏览器语言自动判断、长期保存的手动语言选择、明确本地化 URL 优先级，以及所有非中文语言统一进入英文的既定规则。
- 整合结构化内容回退追踪、稳定 ID 列表、Markdown 正文回退语义、可访问的回退语言标记、本地化占位文字、规范链接、`hreflang` 和英文未完成时的 `noindex` 输出。
- 更新旧 Hash 与 `/about-frc/` 兼容跳转，使其进入对应本地化路由。
- 扩展 Schema 与渲染以支持带类型的图片和视频、可选封面与字幕轨道，同时继续阻止内部参考资料进入可部署产物。
- 调整紧凑标题栏标记和网格，使焦点与视觉顺序依次为 Logo、语言控件、深浅色控件和菜单按钮。
- 保持电脑端网格、`1100 px` 响应式分界、Logo 等比例显示、现有菜单交互，以及向下滚动隐藏和向上滚动返回标题栏的行为不变。
- 通过 `86` 项 framework 测试、无错误／警告／提示的 Astro 诊断，以及分别使用公开和私有内容对全部 `28` 个当前页面进行的生成产物验证。
