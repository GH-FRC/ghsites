# 2026.9.2

## English

### Goals

- Finish framework verification for the three-locale Stable release.
- Finalize the Stable event layout, automatic status behavior, and release verification.
- Fix the persistent Stable dark-mode favicon cache failure without changing the original Logo assets.

### Completed Work

- Verified framework tests, Astro diagnostics, generated-output checks, `zh-Hant` metadata, and Stable-only inactive-route rules.
- Updated framework and architecture documentation to describe the released three-language behavior.
- Balanced localized event headings, added responsive split-image layouts, and ensured foreground artwork uses `object-fit: contain` while the blurred backdrop remains decorative.
- Added scheduled status refreshes and automatic upcoming/history sorting at event boundaries.
- Strengthened build verification for dynamic event routes, transitive JavaScript chunks, language redirects, internal references, image dimensions, and sensitive-content exclusions.
- Built 25 pages and verified 45 output files before and after the direct Stable release.
- Verified source `de42931c4d12172fd3b505c3eca80a6afae89292` on Cloudflare deployment `https://88325920.ghfrc.pages.dev` and the production alias without using GitHub.
- Added a build-time favicon versioning helper that hashes the staged public asset bytes and preserves existing query parameters.
- Replaced the fixed date assertion with generated-output verification that recomputes each light and dark favicon SHA-256 cache key from the deployed file.
- Verified in Chrome dark mode that the page selects and requests `favicon=e48f20f5b242-dark`; confirmed the selected PNG has transparent corners and only white visible pixels.

## 中文

### 当日目标

- 完成 Stable 三语言发布的框架验证。
- 完成 Stable 活动排版、自动状态逻辑及发布验证。
- 在不修改原始 Logo 资源的前提下，修复 Stable 深色模式 favicon 长期复用错误缓存的问题。

### 已完成工作

- 验证框架测试、Astro 检查、生成结果检查、`zh-Hant` 元数据和 Stable 独有的未发布路由规则。
- 更新框架与架构文档，使其准确说明已发布的三语言行为。
- 整理不同语言的活动标题换行，加入响应式图文布局；前景图片使用 `object-fit: contain`，模糊背景仅作为装饰。
- 加入定时状态刷新，并在活动时间边界自动整理“近期活动／历史活动”。
- 加强构建验证，覆盖动态活动路由、间接 JavaScript 文件、语言跳转、内部引用、图片尺寸及敏感内容排除。
- 发布前后均完成 25 页构建与 45 个输出文件验证。
- 在未使用 GitHub 的情况下，核对 Cloudflare 部署 `https://88325920.ghfrc.pages.dev`、正式固定网址及来源 `de42931c4d12172fd3b505c3eca80a6afae89292`。
- 新增构建期 favicon 版本工具，根据已暂存到公开目录的真实文件内容生成摘要，同时保留已有查询参数。
- 将固定日期断言改为生成结果验证：读取每个深浅色 favicon 文件并重新计算 SHA-256 缓存键。
- 在 Chrome 深色模式中确认页面选择并请求 `favicon=e48f20f5b242-dark`；同时核对所选 PNG 四角透明，所有可见像素均为白色。
